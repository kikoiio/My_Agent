# eval/cases/trajectory/multi_step_001.yaml
id: trajectory_music_search_001
name: τ-Bench 风格多步任务 — 搜索+播放音乐
category: trajectory
dimensions: [task_completion, tool_use]
persona: default

setup:
  - load_persona: default

turns:
  - role: user
    content: "帮我搜一下周杰伦的歌，然后放一首他最经典的"

assertions:
  - type: tool_called
    tool: search.bocha_search
    required: true
  - type: tool_called
    tool: pyncm-music.play
    required: true
  - type: llm_judge
    criteria: "Agent 是否按正确顺序先搜索再播放？是否选择了合理的经典歌曲？"
    threshold: 0.8
  - type: metric_range
    metric: SE
    max: 4  # 最短路径应 ≤ 4 步

metrics: [TSR, SE, TSA]

tags:
  - trajectory
  - multi_step
  - music
