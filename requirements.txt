# =============================================================================
# Multi-Persona Voice Agent — Core Dependencies
# =============================================================================
# 安装: pip install -r requirements.txt
#
# 语音硬件依赖（STT/TTS/人脸/声纹）见 requirements-voice.txt：
#   pip install -r requirements-voice.txt
#
# CI 只安装此文件，所有 smoke tests 已 mock 硬件依赖，无需硬件即可运行。
# =============================================================================

# ---- 核心（必装） ----
pydantic>=2.5
PyYAML>=6.0
python-dotenv>=1.0

# ---- Web 服务 & 通信 ----
fastapi>=0.109
uvicorn[standard]>=0.27
websockets>=12.0
aiohttp>=3.9

# ---- LLM 路由 ----
litellm>=1.30
openai>=1.12
anthropic>=0.28

# ---- LangGraph 编排 ----
langgraph>=0.0.40
langchain>=0.1
langchain-core>=0.1

# ---- 评估 & 测试 ----
pytest>=8.0
pytest-asyncio>=0.23
jinja2>=3.1

# ---- TTS（服务端调用） ----
dashscope>=1.17

# ---- TTS Fallback（免费，无需服务器） ----
edge-tts>=6.1             # 微软 Edge TTS，中文语音质量高
miniaudio>=1.2            # MP3/WAV 解码 → PCM 播放

# ---- MCP 服务 ----
bilibili-api-python>=17.0,<18
pyncm>=1.6.10
browser-cookie3>=0.20
caldav>=1.3

# ---- 可选：Mem0 集成 ----
# mem0ai              # 取消注释安装

# ---- [voice] 语音硬件依赖（见 requirements-voice.txt） ----
# faster-whisper        # STT: 本地 Whisper 推理
# sounddevice           # 麦克风采集 / 音频播放
# insightface           # 人脸识别（buffalo_l 模型）
# onnxruntime           # insightface 推理后端
# opencv-python-headless # 图像处理
# resemblyzer           # 声纹验证（GE2E 模型）
# pipewire-python       # Linux 音频路由（可选）

# ---- [optional] 自托管服务 ----
# pipecat-ai          # 可选：Pipecat 音频管线框架
# lark-oapi>=1.3      # 可选：飞书通知
